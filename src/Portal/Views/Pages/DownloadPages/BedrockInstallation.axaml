<UserControl
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d"
    x:Class="Portal.Views.Pages.DownloadPages.BedrockInstallation"
    x:DataType="downloadPages:BedrockInstallationViewModel"
    xmlns="https://github.com/avaloniaui"
    xmlns:bedrock="clr-namespace:Portal.Bedrock.Standard.Interface;assembly=Portal.Bedrock.Standard"
    xmlns:controls="https://github.com/tiouoo/TioUi.Avalonia"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:downloadPages="clr-namespace:Portal.Views.Pages.DownloadPages"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:owned="clr-namespace:Portal.Views.Components"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:loc="using:Portal.Localization">
    <DockPanel>
        <DockPanel
            DockPanel.Dock="Top"
            Margin="0,0,0,12"
            VerticalAlignment="Top">
            <StackPanel
                HorizontalAlignment="Left"
                Orientation="Horizontal"
                VerticalAlignment="Bottom">
                <TextBlock
                    FontSize="20"
                    FontWeight="SemiBold"
                    Text="{Translate {x:Static loc:DownloadsLanguageManager+Keys.bedrockinstallation_gameTitle}}" />
                <TextBlock
                    FontSize="12"
                    Foreground="{DynamicResource InnerForegroundColor}"
                    Margin="8,9,0,0"
                    Text="{Binding StatusText}"
                    VerticalAlignment="Top" />
            </StackPanel>
            <StackPanel
                DockPanel.Dock="Right"
                HorizontalAlignment="Right"
                Orientation="Horizontal"
                Spacing="8"
                VerticalAlignment="Top">
                <ComboBox SelectedIndex="{Binding SelectedReleaseChannel}" Width="105">
                    <ComboBoxItem Content="{Translate {x:Static loc:DownloadsLanguageManager+Keys.bedrockinstallation_releaseChannelAll}}" />
                    <ComboBoxItem Content="{Translate {x:Static loc:DownloadsLanguageManager+Keys.bedrockinstallation_releaseChannelRelease}}" />
                    <ComboBoxItem Content="{Translate {x:Static loc:DownloadsLanguageManager+Keys.bedrockinstallation_releaseChannelPreview}}" />
                </ComboBox>
                <ComboBox SelectedIndex="{Binding SelectedBuildType}" Width="110">
                    <ComboBoxItem Content="{Translate {x:Static loc:DownloadsLanguageManager+Keys.bedrockinstallation_buildTypeAll}}" />
                    <ComboBoxItem Content="UWP" />
                    <ComboBoxItem Content="GDK" />
                </ComboBox>
                <TextBox
                    PlaceholderText="{Translate {x:Static loc:DownloadsLanguageManager+Keys.bedrockinstallation_searchPlaceholder}}"
                    Text="{Binding SearchText}"
                    Width="170">
                    <TextBox.InnerRightContent>
                        <TextBlock
                            FontFamily="{StaticResource IconFont}"
                            FontSize="18"
                            FontWeight="Thin"
                            Foreground="{DynamicResource InnerForegroundColor}"
                            Text="&#xe615;" />
                    </TextBox.InnerRightContent>
                </TextBox>
            </StackPanel>
        </DockPanel>

        <controls:ScrollView>
            <Panel>
            <ItemsRepeater IsVisible="{Binding !IsLoading}" ItemsSource="{Binding Versions}">
                <ItemsRepeater.Layout>
                    <UniformGridLayout
                        ItemsStretch="Fill"
                        MinColumnSpacing="10"
                        MinItemHeight="65"
                        MinItemWidth="270"
                        MinRowSpacing="10"
                        Orientation="Horizontal" />
                </ItemsRepeater.Layout>
                <ItemsRepeater.ItemTemplate>
                    <DataTemplate DataType="bedrock:BedrockVersion">
                        <Border
                            Background="{DynamicResource TranslucentBackgroundColor}"
                            BorderBrush="{DynamicResource TranslucentBorderBrush}"
                            BorderThickness="1"
                            CornerRadius="10"
                            Cursor="Hand"
                            Padding="12"
                            PointerPressed="VersionCard_OnPointerPressed">
                            <StackPanel Spacing="5">
                                <DockPanel>
                                    <TextBlock
                                        FontSize="15"
                                        FontWeight="SemiBold"
                                        Text="{Binding Id}"
                                        TextTrimming="CharacterEllipsis"
                                        VerticalAlignment="Center" />
                                    <StackPanel
                                        DockPanel.Dock="Right"
                                        HorizontalAlignment="Right"
                                        Orientation="Horizontal"
                                        Spacing="5">
                                        <Border
                                            Background="{DynamicResource PopupBackgroundColor}"
                                            BorderBrush="{DynamicResource TranslucentBorderBrush}"
                                            BorderThickness="1"
                                            CornerRadius="8"
                                            HorizontalAlignment="Right"
                                            Padding="5,3,5,1"
                                            VerticalAlignment="Top">
                                            <TextBlock
                                                FontSize="11"
                                                Foreground="{DynamicResource InnerForegroundColor}"
                                                Text="{Binding ChannelLabel}" />
                                        </Border>
                                        <Border
                                            Background="{DynamicResource PopupBackgroundColor}"
                                            BorderBrush="{DynamicResource TranslucentBorderBrush}"
                                            BorderThickness="1"
                                            CornerRadius="8"
                                            Padding="5,3,5,1"
                                            VerticalAlignment="Top">
                                            <TextBlock
                                                FontSize="11"
                                                FontWeight="SemiBold"
                                                Text="{Binding BuildLabel}" />
                                        </Border>
                                    </StackPanel>
                                </DockPanel>
                                <TextBlock FontSize="12" Foreground="{DynamicResource InnerForegroundColor}">
                                    <Run Text="{Binding RelativeReleaseTime}" />
                                </TextBlock>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsRepeater.ItemTemplate>
            </ItemsRepeater>
                <owned:GameVersionSkeleton IsVisible="{Binding IsLoading}" />
            </Panel>
        </controls:ScrollView>
    </DockPanel>
</UserControl>
