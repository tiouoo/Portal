<UserControl
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d"
    x:Class="Portal.Views.Pages.SettingPage"
    x:DataType="pages:SettingPageViewModel"
    xmlns="https://github.com/avaloniaui"
    xmlns:controls="https://github.com/tiouoo/TioUi.Avalonia"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:p="clr-namespace:Portal.Views.Pages.SettingPages"
    xmlns:pages="clr-namespace:Portal.Views.Pages"
    xmlns:tio="https://github.com/tiouoo/TioUi.Avalonia"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:loc="using:Portal.Localization">
    <DockPanel>
        <controls:ScrollView
            Classes="Flat"
            DockPanel.Dock="Left"
            HorizontalAlignment="Left"
            Margin="7,0,10,10"
            Width="180">
            <tio:NavMenu
                Margin="0,0,10,0"
                Name="NavMenu"
                Theme="{StaticResource FlatScrollBarTheme}">
                <tio:NavMenuItem Header="{Translate {x:Static loc:PagesLanguageManager+Keys.settingpage_0}}">
                    <tio:NavMenuItem
                        Command="{Binding NavigateTypeCommand}"
                        CommandParameter="{x:Type p:Appearance}"
                        Header="{Translate {x:Static loc:PagesLanguageManager+Keys.settingpage_1}}"
                        IsSelected="True" />
                    <tio:NavMenuItem
                        Command="{Binding NavigateTypeCommand}"
                        CommandParameter="{x:Type p:DefaultBehavior}"
                        Header="{Translate {x:Static loc:PagesLanguageManager+Keys.settingpage_2}}" />
                    <tio:NavMenuItem
                        Command="{Binding NavigateTypeCommand}"
                        CommandParameter="{x:Type p:Shortcuts}"
                        Header="{Translate {x:Static loc:PagesLanguageManager+Keys.settingpage_3}}" />
                    <tio:NavMenuItem
                        Command="{Binding NavigateTypeCommand}"
                        CommandParameter="{x:Type p:Notification}"
                        Header="{Translate {x:Static loc:PagesLanguageManager+Keys.settingpage_4}}" />
                    <tio:NavMenuItem
                        Command="{Binding NavigateTypeCommand}"
                        CommandParameter="{x:Type p:Storage}"
                        Header="{Translate {x:Static loc:PagesLanguageManager+Keys.settingpage_5}}" />
                    <tio:NavMenuItem
                        Command="{Binding NavigateTypeCommand}"
                        CommandParameter="{x:Type p:ApplicationDebug}"
                        Header="{Translate {x:Static loc:PagesLanguageManager+Keys.settingpage_6}}" />
                    <tio:NavMenuItem
                        Command="{Binding NavigateTypeCommand}"
                        CommandParameter="{x:Type p:About}"
                        Header="{Translate {x:Static loc:PagesLanguageManager+Keys.settingpage_7}}"
                        Name="AboutItem" />
                </tio:NavMenuItem>
                <tio:NavMenuItem Header="{Translate {x:Static loc:PagesLanguageManager+Keys.settingpage_8}}">
                    <tio:NavMenuItem
                        Command="{Binding NavigateTypeCommand}"
                        CommandParameter="{x:Type p:Account}"
                        Header="{Translate {x:Static loc:PagesLanguageManager+Keys.settingpage_9}}" />
                    <tio:NavMenuItem
                        Command="{Binding NavigateTypeCommand}"
                        CommandParameter="{x:Type p:Java}"
                        Header="{Translate {x:Static loc:PagesLanguageManager+Keys.settingpage_10}}" />
                    <tio:NavMenuItem
                        Command="{Binding NavigateTypeCommand}"
                        CommandParameter="{x:Type p:GameFolder}"
                        Header="{Translate {x:Static loc:PagesLanguageManager+Keys.settingpage_11}}" />
                    <tio:NavMenuItem
                        Command="{Binding NavigateTypeCommand}"
                        CommandParameter="{x:Type p:Advanced}"
                        Header="{Translate {x:Static loc:PagesLanguageManager+Keys.settingpage_12}}" />
                </tio:NavMenuItem>
                <tio:NavMenuItem Header="{Translate {x:Static loc:PagesLanguageManager+Keys.settingpage_13}}">
                    <tio:NavMenuItem
                        Command="{Binding NavigateTypeCommand}"
                        CommandParameter="{x:Type p:Download}"
                        Header="{Translate {x:Static loc:PagesLanguageManager+Keys.settingpage_14}}" />
                    <tio:NavMenuItem
                        Command="{Binding NavigateTypeCommand}"
                        CommandParameter="{x:Type p:Agent}"
                        Header="{Translate {x:Static loc:PagesLanguageManager+Keys.settingpage_15}}" />
                </tio:NavMenuItem>
            </tio:NavMenu>
        </controls:ScrollView>
        <controls:ScrollView Margin="0,0,5,0">
            <TransitioningContentControl
                Content="{Binding CurrentPage}"
                Margin="0,10,10,10"
                MaxWidth="820"
                Name="Frame">
                <TransitioningContentControl.PageTransition>
                    <CrossFade Duration="0:0:0.2" />
                </TransitioningContentControl.PageTransition>
            </TransitioningContentControl>
        </controls:ScrollView>
    </DockPanel>
</UserControl>