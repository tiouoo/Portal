<UserControl
    d:DesignHeight="520"
    d:DesignWidth="560"
    mc:Ignorable="d"
    x:Class="Portal.Views.Pages.InstancePages.ResourceVersionSwitchDialog"
    x:DataType="instancePages:ResourceVersionSwitchDialogViewModel"
    xmlns="https://github.com/avaloniaui"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:instancePages="clr-namespace:Portal.Views.Pages.InstancePages"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:loc="using:Portal.Localization">
    <Grid
        Margin="24"
        MaxHeight="625"
        RowDefinitions="Auto, Auto, Auto, *, Auto"
        Width="560">
        <DockPanel Grid.Row="0">
            <Panel DockPanel.Dock="Left">
                <TextBlock
                    FontSize="16"
                    FontWeight="600"
                    HorizontalAlignment="Left"
                    Text="{Translate {x:Static loc:InstancesLanguageManager+Keys.resourceversionswitchdialog_title}}" />
            </Panel>
            <TextBlock
                DockPanel.Dock="Right"
                Foreground="{DynamicResource InnerForegroundColor}"
                IsVisible="{Binding IsLoading}"
                Text="{Translate {x:Static loc:InstancesLanguageManager+Keys.resourceversionswitchdialog_loading}}" />
            <TextBlock
                DockPanel.Dock="Right"
                Foreground="{DynamicResource InnerForegroundColor}"
                IsVisible="{Binding HasError}"
                Text="{Translate {x:Static loc:InstancesLanguageManager+Keys.resourceversionswitchdialog_loadError}}" />
        </DockPanel>
        <CheckBox
            Content="{Translate {x:Static loc:InstancesLanguageManager+Keys.resourceversionswitchdialog_showAll}}"
            Grid.Row="1"
            IsChecked="{Binding ShowAll}"
            Margin="0,10,0,0" />
        <TextBox
            Grid.Row="2"
            Margin="0,8,0,0"
            PlaceholderText="{Translate {x:Static loc:InstancesLanguageManager+Keys.resourceversionswitchdialog_searchPlaceholder}}"
            Text="{Binding SearchText}">
            <TextBox.InnerRightContent>
                <PathIcon
                    Data="{StaticResource SearchGeometry}"
                    Foreground="{DynamicResource InnerForegroundColor}"
                    Height="14"
                    Width="14" />
            </TextBox.InnerRightContent>
        </TextBox>
        <Border
            Grid.Row="3"
            Margin="0,8,0,8"
            Padding="1">
            <ListBox
                ItemsSource="{Binding VisibleItems}"
                MaxHeight="380"
                Name="VersionListBox"
                SelectedItem="{Binding SelectedItem, Mode=TwoWay}">
                <ListBox.Styles>
                    <Style Selector="ListBoxItem">
                        <Setter Property="Margin" Value="0,2" />
                        <Setter Property="Padding" Value="0" />
                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    </Style>
                </ListBox.Styles>
                <ListBox.ItemTemplate>
                    <DataTemplate x:DataType="instancePages:ResourceVersionItem">
                        <Border
                            CornerRadius="6"
                            Padding="8,5">
                            <StackPanel Spacing="1">
                                <DockPanel>
                                    <Border
                                        Background="{DynamicResource SystemAccentColor}"
                                        CornerRadius="8"
                                        DockPanel.Dock="Left"
                                        IsVisible="{Binding IsCurrent}"
                                        Margin="0,0,6,1"
                                        Padding="5,1,5,0">
                                        <TextBlock
                                            FontSize="11"
                                            Foreground="White"
                                            Text="{Translate {x:Static loc:InstancesLanguageManager+Keys.resourceversionswitchdialog_currentVersion}}"
                                            VerticalAlignment="Center" />
                                    </Border>
                                    <TextBlock
                                        FontSize="13"
                                        FontWeight="SemiBold"
                                        MaxLines="1"
                                        Text="{Binding File.DisplayName}"
                                        TextTrimming="CharacterEllipsis" />
                                </DockPanel>
                                <TextBlock
                                    FontSize="11"
                                    Foreground="{DynamicResource InnerForegroundColor}"
                                    MaxLines="1"
                                    Text="{Binding File.Details}"
                                    TextTrimming="CharacterEllipsis" />
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Border>
        <StackPanel
            Grid.Row="4"
            HorizontalAlignment="Right"
            Orientation="Horizontal"
            Spacing="8">
            <Button Command="{Binding CancelCommand}" Content="{Translate {x:Static loc:InstancesLanguageManager+Keys.resourceversionswitchdialog_cancel}}" />
            <Button
                Command="{Binding ConfirmCommand}"
                Content="{Translate {x:Static loc:InstancesLanguageManager+Keys.resourceversionswitchdialog_switchToVersion}}"
                IsEnabled="{Binding CanConfirm}" />
        </StackPanel>
    </Grid>
</UserControl>
