<UserControl
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d"
    x:Class="Portal.Views.Pages.SettingPages.Download"
    x:DataType="settingPages:Download"
    xmlns="https://github.com/avaloniaui"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:settingPages="clr-namespace:Portal.Views.Pages.SettingPages"
    xmlns:tio="https://github.com/tiouoo/TioUi.Avalonia"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:loc="using:Portal.Localization">
    <StackPanel Spacing="15">
        <TextBlock FontSize="26" Text="{Translate {x:Static loc:SettingsLanguageManager+Keys.download_0}}" />
        <StackPanel Spacing="8">
            <TextBlock FontSize="18" Text="{Translate {x:Static loc:SettingsLanguageManager+Keys.download_1}}" />
            <StackPanel Spacing="5">
                <TextBlock Text="{Translate {x:Static loc:SettingsLanguageManager+Keys.download_2}}" VerticalAlignment="Center" />
                <ComboBox
                    HorizontalAlignment="Stretch"
                    ItemsSource="{Binding SourceOptions}"
                    SelectedValue="{Binding Data.ConfigEntry.MinecraftMetadataSource}"
                    SelectedValueBinding="{Binding Mode}">
                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Name}" />
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>
            </StackPanel>
            <StackPanel Spacing="5">
                <TextBlock Text="{Translate {x:Static loc:SettingsLanguageManager+Keys.download_3}}" VerticalAlignment="Center" />
                <ComboBox
                    HorizontalAlignment="Stretch"
                    ItemsSource="{Binding SourceOptions}"
                    SelectedValue="{Binding Data.ConfigEntry.MinecraftFileSource}"
                    SelectedValueBinding="{Binding Mode}">
                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Name}" />
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>
            </StackPanel>
        </StackPanel>
        <StackPanel Spacing="5">
            <Label
                Content="{Translate {x:Static loc:SettingsLanguageManager+Keys.download_4}}"
                FontSize="13"
                Foreground="{DynamicResource InnerForegroundColor}" />
            <Grid ColumnDefinitions="*,Auto">
                <Slider
                    Grid.Column="0"
                    IsSnapToTickEnabled="True"
                    Maximum="512"
                    Minimum="1"
                    TickFrequency="1"
                    Value="{Binding Data.ConfigEntry.DownloadMaxThreadCount}" />
                <TextBlock
                    Grid.Column="1"
                    Margin="6,0,0,0"
                    Text="{Binding Data.ConfigEntry.DownloadMaxThreadCount, StringFormat={}{0:0}}"
                    VerticalAlignment="Center"
                    Width="40" />
            </Grid>
        </StackPanel>
        <StackPanel Spacing="5">
            <Label
                Content="{Translate {x:Static loc:SettingsLanguageManager+Keys.download_5}}"
                FontSize="13"
                Foreground="{DynamicResource InnerForegroundColor}" />
            <Grid ColumnDefinitions="*,Auto">
                <Slider
                    Grid.Column="0"
                    IsSnapToTickEnabled="True"
                    Maximum="20"
                    Minimum="0"
                    TickFrequency="1"
                    Value="{Binding Data.ConfigEntry.DownloadMaxRetryCount}" />
                <TextBlock
                    Grid.Column="1"
                    Margin="6,0,0,0"
                    Text="{Binding Data.ConfigEntry.DownloadMaxRetryCount, StringFormat={}{0:0}}"
                    VerticalAlignment="Center"
                    Width="40" />
            </Grid>
        </StackPanel>
        <StackPanel Spacing="5">
            <Label
                Content="{Translate {x:Static loc:SettingsLanguageManager+Keys.download_6}}"
                FontSize="13"
                Foreground="{DynamicResource InnerForegroundColor}" />
            <ToggleSwitch IsChecked="{Binding Data.ConfigEntry.EnableFragmentDownload}" />
        </StackPanel>
        <StackPanel IsVisible="{Binding Data.ConfigEntry.EnableFragmentDownload}" Spacing="5">
            <Label
                Content="{Translate {x:Static loc:SettingsLanguageManager+Keys.download_7}}"
                FontSize="13"
                Foreground="{DynamicResource InnerForegroundColor}" />
            <Grid ColumnDefinitions="*,Auto">
                <Slider
                    Grid.Column="0"
                    IsSnapToTickEnabled="True"
                    Maximum="512"
                    Minimum="1"
                    TickFrequency="1"
                    Value="{Binding Data.ConfigEntry.DownloadMaxFragmentCount}" />
                <TextBlock
                    Grid.Column="1"
                    Margin="6,0,0,0"
                    Text="{Binding Data.ConfigEntry.DownloadMaxFragmentCount, StringFormat={}{0:0}}"
                    VerticalAlignment="Center"
                    Width="40" />
            </Grid>
        </StackPanel>
        <tio:Banner
            Classes="Bordered"
            Content="{Translate {x:Static loc:SettingsLanguageManager+Keys.download_8}}"
            CornerRadius="10"
            HorizontalContentAlignment="Left"
            IsVisible="{Binding HasHighConcurrencyWarning}"
            Margin="0,10,0,15"
            Type="Warning">
            <tio:Banner.Header>
                <TextBlock FontSize="15" Text="{Translate {x:Static loc:SettingsLanguageManager+Keys.download_9}}" />
            </tio:Banner.Header>
        </tio:Banner>
    </StackPanel>
</UserControl>